services:
  app:
    build:
      target: prod
    restart: always
    volumes: !reset []
    command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]
    environment:
      DEBUG: "false"

  db:
    restart: always
    ports: !reset []
